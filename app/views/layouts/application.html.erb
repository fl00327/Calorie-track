<!DOCTYPE html>
<!-- Pushing the code to master repositry on github if you want to check https://github.com/fl00327/Calorie-track  -->
<html>
  <head>
    <title>Calorie-Track</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <!--
    Using Bulma CSS Framework Just like bootstrap and getting the templates in the application so I can use in other views
    -->
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.1/css/bulma.min.css">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <!--  rendering the header for every view  -->
    <%= render "shared/header" %>

    <% if notice.present? %>
       <div class="notification is-link is-light"><%= notice %></div>
    <% end %>

    <% if alert.present? %>
       <div class="notification is-danger is-light"><%= alert %></div>
    <% end %>

    <div class="container">
    <%= yield %>

    <%= render "shared/footer" %>
    </div>

  </body>
</html>
